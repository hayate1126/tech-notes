## 🧪 テスト戦略 (Testing Strategy)（仮）

「テストのためのテスト」を避け、**「リファクタリングの勇気を与え、バグの混入を最小化する」**ことを目的に、3つのレイヤーでテストを構成しています。

### 1. テスト・ピラミッドとROI

| レイヤー | 対象 | ツール | 目的 |
| :--- | :--- | :--- | :--- |
| **Unit Test** | `logic.ts` (純粋関数) | Vitest | ビジネスルールの正当性を100%保証する |
| **Visual Test** | `components/ui` (Shared) | Storybook | 意図しない表示崩れ・デグレの検知 |
| **E2E Test** | 重要なユーザーフロー | Playwright | ユーザーの主要シナリオが完遂できることの保証 |



### 2. 各レイヤーの注力ポイント

#### ✅ Domain Logic (Unit Test)
最もROIが高いテストです。Reactから切り離された `logic.ts` 内の純粋関数をテストします。
- **ポイント:** 条件分岐の全パターン、境界値（例：18歳と19歳の判定）の網羅。
- **メリット:** 実行速度が極めて速く、仕様変更時の影響確認が容易です。

#### ✅ Shared UI (Visual Regression Test)
ドメイン知識を持たない汎用パーツのテストです。
- **ポイント:** ボタンの各ステータス（Disabled, Loading）、ダークモード、レスポンシブ表示。
- **メリット:** ロジックがないUIパーツに対してコードでテストを書く手間を省き、視覚的に担保します。

#### ✅ Feature & Integration (E2E Test)
「ログインして投稿し、タイムラインに反映される」といった、複数の機能が連携する「点ではなく線の動き」をテストします。
- **ポイント:** ユーザーにとっての「価値」に直結するハッピーパス（主要成功シナリオ）のみに絞ります。
- **メリット:** APIとの接続を含めた、本番に近い環境での安心感を確保します。

### 3. 「テストしないこと」の定義
メンテナンスコストを抑えるため、以下の項目は原則としてテストを書きません。
- **自明なコード:** `props` をそのまま表示するだけの単純なコンポーネント。
- **外部ライブラリの動作:** ライブラリ自体の機能（例：Zodが正しくパースするか等）の検証。
- **頻繁に変更されるUIの詳細:** PaddingやColorなどの微細な調整（Visual Testで吸収）。

### 4. テストコードの例 (Unit Test)
```typescript
// features/post/logic.test.ts
import { calculateDisplayDate } from './logic';

describe('calculateDisplayDate', () => {
  it('1分未満なら「たった今」と返すこと', () => {
    const now = new Date();
    expect(calculateDisplayDate(now)).toBe('たった今');
  });
  // ...その他のビジネスルール
});